service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true; // Allows everyone to read
      allow write: if request.auth != null; // Allows authenticated users to write
    }
  }
}

